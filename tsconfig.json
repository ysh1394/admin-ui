{
  "compilerOptions": {
    "target": "esnext", // 어떤 버전의 js를 변환 할 지
    "lib": ["dom", "dom.iterable", "esnext"], // 타입스크립트 파일을 자바스크립트로 컴파일 할 때 포함될 라이브러리의 목록
    "allowJs": true, // js파일도 허용
    "noImplicitAny": true, // any 타입이 의도치 않게 발생했을 떄 에러
    "strictNullChecks": true, // null, undefined 등을 이상하게 조작할떄
    "removeComments": true, // 컴파일 시, 주석 제거
    "typeRoots": ["./src/types", "./node_modules/@types"], // 타입 정의를 포함할 폴더 목록, 설정 안 할 시 기본적으로 ./node_modules/@types로 설정
    "skipLibCheck": true, // 사용하는 라이브러리의 파일의 타입 검사를 skip
    "strict": false, // 엄격모드
    "forceConsistentCasingInFileNames": true, // 같은 파일에 대한 일관되지 않은 참조를 허용하지 않을 지 여부
    "noEmit": true, // 단순 타임 체크용으로 사용할 것인지 아니면 tsc를 컴파일용으로 사용할 것인지 지정
    "incremental": true, // 컴파일 기록들을 남겨둬서 필요한것만 효율적으로 컴파일 되도록 하는 기능
    "esModuleInterop": true, // import 사용 가능
    "module": "esnext", // 컴파일된 결과물이 사용하게 될 module 방식
    "moduleResolution": "node", // nodejs 기반으로 사용
    "resolveJsonModule": true, // .json으로 된 파일의 모듈을 import 할 수 있께 해준다.
    "isolatedModules": true, // 프로젝트 내에 모든 각각의 소스코드 파일을 모듈로 만들기를 강제 (import, export)
    "jsx": "react-jsx", // tsx 파일을 jsx로 어떻게 컴파일할 것인지 'preserve', 'react-native', 'react'
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
